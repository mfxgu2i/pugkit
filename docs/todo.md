# 既知の改善点（TODO）

| 優先度 | 場所          | 問題                                                                                                                                                                      |
| ------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🔴 高  | `watcher.mjs` | Pug パーシャル削除時（`onPugUnlink`）に、そのパーシャルを参照していた親ファイルの再ビルドがない。削除後も古い HTML が残り続けるためエラーが表面化しない                   |
| 🟡 中  | `builder.mjs` | `clean()` が `graph` と `cache` のみクリアしており、`sassGraph`・`scriptGraph`・`imageGraph` が放置される（build では実害なし、dev の再起動なし運用では残骸が残る可能性） |
| 🟡 中  | `cache.mjs`   | `computeHash` がファイル読み込みに失敗した場合に `error-${Math.random()}` を返す設計になっており、呼び出すたびにハッシュが変わるため不要な再ビルドが発生しうる            |
| 🟢 低  | `context.mjs` | `this.patterns`（`createGlobPatterns` の戻り値）がどのタスクからも参照されていないデッドコード                                                                            |
